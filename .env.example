PORT=5000
# Cron for water reminder job (default: every minute for testing)
# SCHEDULER_CRON=* * * * *
# SCHEDULER_TZ=America/Los_Angeles

SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key

TWILIO_ACCOUNT_SID=your_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token

# Optional overrides for GET /test-whatsapp
# GET /test-whatsapp (quote so "+" is never stripped by a parser)
TEST_WHATSAPP_TO="+919629071076"

# TEST_WHATSAPP_MESSAGE=Hello from test route
# Twilio WhatsApp `from` (sandbox default below)
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886


